# Tarea 07: Github

Esta carpeta contiene la evidencia de la Tarea 07.

## Comandos Ejecutados (Simulación)

```bash
# Vincular repositorio remoto
git remote add origin https://github.com/welink/fdd_p25.git
git remote -v

# Subir cambios (Push)
git branch -M main
git push -u origin main

# Flujo de trabajo (Branching)
git checkout -b feature/nueva-funcionalidad
# ... hacer cambios ...
git add .
git commit -m "Agregada nueva funcionalidad"
git push origin feature/nueva-funcionalidad

# Pull Request
# (Se realiza desde la interfaz web de GitHub)
```

## Notas
Esta tarea demuestra el conocimiento de los comandos para interactuar con repositorios remotos en GitHub.
